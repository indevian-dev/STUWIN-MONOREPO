// ═══════════════════════════════════════════════════════════════
// UTILITIES - CENTRAL EXPORTS
// ═══════════════════════════════════════════════════════════════
// Type utilities for UI components (generic helpers, mapped types, etc.)

// Example utilities that can be added:
// export type FormState<T> = { data: T; isValid: boolean; errors: Record<keyof T, string> };
// export type ComponentState<T> = 'idle' | 'loading' | 'success' | 'error';
// export type AsyncData<T> = { data: T | null; loading: boolean; error: Error | null };

export {};

